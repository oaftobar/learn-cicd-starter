on:
  push:
    branches: [main]

jobs:
  deploy:
  runs-on: ubuntu-latest
  
    steps:
    - name: Check out code
      uses: action/checkout@v4
    
    - name: Set up Go
      uses: action/setup-go@v5

    - name: Build the app
      uses: .scripts/buildprod.sh
